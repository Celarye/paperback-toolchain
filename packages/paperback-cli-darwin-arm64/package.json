{
  "name": "@paperback/paperback-cli-darwin-arm64",
  "version": "1.0.0",
  "os": ["darwin"],
  "cpu": ["arm", "arm64"],
  "scripts": {
    "build": "env GOOS=darwin GOARCH=arm64 go build ../paperback-cli/main.go -o ./bin/paperback-cli -ldflags \"-w\""
  }
}